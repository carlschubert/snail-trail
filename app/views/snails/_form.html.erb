<%= form_with(model: snail, local: true) do |form| %>
  <% if snail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(snail.errors.count, "error") %> prohibited this snail from being saved:</h2>

      <ul>
        <% snail.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :species %>
    <%= form.text_field :species %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
